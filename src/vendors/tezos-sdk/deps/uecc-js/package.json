{
  "name": "@nomadic-labs/uecc-js",
  "version": "1.0.6",
  "main": "uECC.js",
  "license": "MIT",
  "scripts": {
    "build:emscripten": "docker run -u $(id -u) --rm -v $(pwd)/src:/src trzeci/emscripten-slim ./build.sh",
    "build:app": "cp src/uECC.wasm src/uECC.js .",
    "build": "npm run build:emscripten && npm run build:app",
    "test": "mocha"
  },
  "devDependencies": {
    "mocha": "*"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/tezos/tezos.git",
    "directory": "vendors/ocaml-uecc"
  },
  "files": [ "uECC.wasm", "uECC.js" ]
}
